import 'package:flutter/material.dart';

class HelmetDetectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Helmet Detection')),
      body: Center(child: Text('Helmet Detection Module Here')),
    );
  }
}
