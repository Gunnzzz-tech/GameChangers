import 'package:flutter/material.dart';

class SpeedDetectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Speed Detection')),
      body: Center(child: Text('Speed Detection Module Here')),
    );
  }
}
