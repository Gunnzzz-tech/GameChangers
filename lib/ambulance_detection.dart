import 'package:flutter/material.dart';

class AmbulanceDetectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ambulance Detection')),
      body: Center(child: Text('Ambulance Detection Module Here')),
    );
  }
}
